"""Constants for runboat."""

SOURCE_DB_USE_LAST = "last"  # Value to search for the last DB of the branch/PR
SOURCE_DB_TOPIC = "runboat-source-db"  # GitHub topic: if repo has it, webhook uses source_db="last" with silent fallback
